{
  "name": "taskcli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": { "taskcli": "src/cli.ts" },
  "scripts": {
    "dev": "bun run src/cli.ts",
    "test": "bun test",
    "build": "bun build src/cli.ts --compile --outfile dist/taskcli"
  },
  "devDependencies": {
    "bun-types": "^1.3.11"
  }
}
